// const express = require('express');
// const router = express.Router();
// const { createComplaint, listComplaints, updateComplaint, deleteComplaint } = require('../controllers/complaintController');
// const { isAuthenticated } = require('../middleware/auth');  // Assuming you have authentication middleware

// // Create a new complaint
// router.post('cretae/', isAuthenticated, createComplaint);

// // List all complaints for the logged-in user
// router.get('listcomplaint/', isAuthenticated, listComplaints);

// // Update complaint status
// router.put('/:complaintID', isAuthenticated, updateComplaint);

// // Delete complaint
// router.delete('/:complaintID', isAuthenticated, deleteComplaint);

// module.exports = router;
